---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: media
resources:
  - slsk-auth.yaml
  - release.yaml
configMapGenerator:
  - name: slskd-values
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml
