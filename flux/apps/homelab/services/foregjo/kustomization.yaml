---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: services
resources:
  - database.yaml
  - forgejo-admin.yaml
  - release.yaml
configMapGenerator:
  - name: forgejo-values
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml
