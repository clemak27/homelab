---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/refs/heads/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: public-test
  namespace: services
spec:
  interval: 50m
  chart:
    spec:
      chart: app-template
      version: 5.1.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s-labs
      interval: 50m
  releaseName: public-test
  targetNamespace: services
  driftDetection:
    mode: enabled
  valuesFrom:
    - kind: ConfigMap
      name: public-test-values
      valuesKey: values.yaml
