---
variant: fcos
version: 1.4.0
storage:
  files:
    - path: /usr/local/sbin/initNixTB
      mode: 0755
      contents:
        inline: |
          #!/bin/bash
          echo "Creating toolbox..."
          toolbox create -y nix
          echo "Installing nix inside toolbox..."
          toolbox run -c nix ~/Projects/homelab/modules/nix/create_nix_toolbox.sh
