variant: fcos
version: 1.4.0
ignition:
  config:
    merge:
      - local: hosts/autoupdates.ign
      - local: hosts/i18n.ign
      - local: hosts/overlays.ign
      - local: hosts/user.ign
storage:
  files:
    - path: /etc/hostname
      mode: 0644
      contents:
        inline: nuke

