variant: fcos
version: 1.4.0
ignition:
  config:
    merge:
      - local: hosts/user.ign
      - local: hosts/overlays.ign
      - local: hosts/i18n.ign
storage:
  files:
    - path: /etc/hostname
      mode: 0644
      contents:
        inline: virtual

