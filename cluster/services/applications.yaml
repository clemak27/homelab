---
apiVersion: v1
kind: Namespace
metadata:
  name: services
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: git
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/clemak27/homelab
        revision: master
        directories:
          - path: cluster/services/vaultwarden
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
      source:
        repoURL: https://github.com/clemak27/homelab
        targetRevision: master
        path: '{{path}}'
        helm:
          valueFiles:
            - values.yaml
            - secrets+age-import-kubernetes://argocd/helm-secrets-private-keys#key.txt?https://raw.githubusercontent.com/clemak27/homelab/master/{{path}}/secrets.yaml
      destination:
        name: in-cluster
        namespace: services
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: services
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/clemak27/homelab
        revision: master
        directories:
          # - path: cluster/services/*
          - path: cluster/services/cloudflare-ddns
          - path: cluster/services/deconz
          - path: cluster/services/filebrowser
          - path: cluster/services/gitea
          - path: cluster/services/home-assistant
          - path: cluster/services/jackett
          - path: cluster/services/jellyfin
          - path: cluster/services/miniflux
          - path: cluster/services/qobuz-dl
          - path: cluster/services/radarr
          - path: cluster/services/recipes
          - path: cluster/services/sonarr
          - path: cluster/services/syncthing
          - path: cluster/services/transmission
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
      source:
        repoURL: https://github.com/clemak27/homelab
        targetRevision: master
        path: '{{path}}'
      destination:
        name: in-cluster
        namespace: services
