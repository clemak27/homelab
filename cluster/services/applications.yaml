---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: services
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/clemak27/homelab
        revision: feat/dnsmasq-k8s
        directories:
          - path: cluster/services/*

  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
      source:
        repoURL: https://github.com/clemak27/homelab
        targetRevision: feat/dnsmasq-k8s
        path: '{{path}}'
      destination:
        name: in-cluster
        namespace: services
