---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/postgresql.cnpg.io/cluster_v1.json
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: recipes-database-test
spec:
  imageName: ghcr.io/cloudnative-pg/postgresql:16.3
  instances: 3
  bootstrap:
    initdb:
      database: recipes
      owner: recipes
  minSyncReplicas: 1
  maxSyncReplicas: 1
  storage:
    # recipes-database-test-X
    storageClass: longhorn-crypto
    size: 1Gi
    pvcTemplate:
      selector:
        matchLabels:
          cnpg-data: recipes-database-test
