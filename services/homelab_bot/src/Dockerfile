FROM golang:1.19.4-alpine

ARG TELEGRAM_CHAT
ARG TELEGRAM_URL

RUN mkdir /app
COPY . /app
WORKDIR /app

ENV TELEGRAM_CHAT "$TELEGRAM_CHAT"
ENV TELEGRAM_URL "$TELEGRAM_URL"

EXPOSE 8525

RUN go build -o main .
CMD ["/app/main"]
