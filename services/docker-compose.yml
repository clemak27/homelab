version: '3.8'

networks:
  private:
    name: private
    external: true
    ipam:
      config:
        - subnet: "172.19.0.0/24"
  internal:
    name: internal
    external: true
  public:
    name: public
    external: true
