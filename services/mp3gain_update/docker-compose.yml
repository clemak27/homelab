---
version: '3.8'

services:
  mp3gain_update:
    container_name: mp3gain_update
    image: mp3gain_update:latest
    build:
      context: mp3gain_update/src
    volumes:
      - "${MUSIC_DATA}:/app/music"
    networks:
      - private
    restart: unless-stopped
