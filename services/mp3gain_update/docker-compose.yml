version: '3.8'

services:
  mp3gain_update:
    build:
      context: src
    container_name: mp3gain_update
    volumes:
      - "${DOCKER_DATA}/jellyfin/media/music:/app/music"
    networks:
      - private
    restart: unless-stopped
