---
name: deploy_test
on:
  - pull_request
jobs:
  deploy_test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.0.0
      - name: Test deployment
        run: cd services &&
             export DEPLOY_TEST=true &&
             ./deploy.sh
