---
name: deploy_test
on:
  - pull_request
jobs:
  deploy_test:
    runs-on: ubuntu-latest
    env:
      DEPLOY_TEST: true
    steps:
      - name: Checkout
        uses: actions/checkout@v2.0.0
      - name: Test deployment
        run: cd services && ./deploy.sh
